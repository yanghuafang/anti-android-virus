#ifndef AAV_AAV_H_
#define AAV_AAV_H_

// Umbrella header for the public aav SDK API. The engine is driven through the
// high-level facade (aav::MakeEngine returning aav::IEngine*); IObject provides
// the ABI-safe Destroy() teardown for the objects it hands back.

#include "aav/engine_interface.h"
#include "aav/object_interface.h"

#endif
